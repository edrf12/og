{
  "name": "og",
  "version": "0.0.1",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "npm run build && netlify deploy --build --context deploy-preview",
    "deploy": "npm run build && netlify deploy"
  },
  "devDependencies": {
    "@fontsource/fira-mono": "^4.5.10",
    "@sveltejs/adapter-auto": "^3.0.0",
    "@sveltejs/adapter-netlify": "^4.2.0",
    "@sveltejs/kit": "^2.0.0",
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "fernet": "^0.3.2",
    "svelte": "^4.2.7",
    "svelte-check": "^3.6.0",
    "typescript": "^5.4.5",
    "vite": "^5.0.3"
  },
  "type": "module"
}